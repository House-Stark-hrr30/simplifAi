// import node modules
import { Router } from 'express';

// initialize express router
const route = Router();

// route.get('/', (req, res) => {
//   res.send(index.html);
// });

export default route;